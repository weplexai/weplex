# Apple signing (macOS only) — put your Developer ID password in .env, not here.
APPLE_ID=
APPLE_PASSWORD=
APPLE_TEAM_ID=

# PostHog (optional — analytics and feature flags).
# Must match the weplex-web project key so landing and app events go to
# the same person profiles and we can see the full funnel.
# If VITE_POSTHOG_KEY is not set, analytics is a no-op and feature flags
# return their fallbacks (off for alpha).
VITE_POSTHOG_KEY=phc_mDezXgR8Hek7iQ5dokrhrYLD8QyfJUBkdprMLyvzRYS7
VITE_POSTHOG_HOST=https://ph.weplex.ai
